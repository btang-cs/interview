name: Privacy Scan

on:
  push:
    branches:
      - main
      - "codex/**"
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run privacy scan
        run: bash scripts/privacy_scan.sh --all
